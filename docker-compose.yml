version: '3'
services:
  gstreamer:
    build:
      dockerfile: docker/Dockerfile
      context: .
    volumes:
      - ./:/home/Projects/
    working_dir: /home/Projects/
    entrypoint:
      - /bin/bash
